<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.kouko.intellij-dbtree</id>
    <name>dbtree</name>
    <vendor email="kouko.d@gmail.com" url="https://github.com/kouko/intellij-dbtree">kouko</vendor>

    <description><![CDATA[
        <p><b>dbtree</b> visualizes <a href="https://www.getdbt.com/">dbt</a> model lineage inside JetBrains IDEs (DataSpell, PyCharm, IntelliJ IDEA), with <b>column-level lineage</b> powered by <a href="https://github.com/tobymao/sqlglot">sqlglot</a>.</p>
        <ul>
            <li>Interactive model-level DAG via JCEF + React Flow</li>
            <li>Column-to-column lineage tracing across CTEs, JOINs, UNIONs, window functions</li>
            <li>Successor of the archived <code>ramonvermeulen/dbt-toolkit</code></li>
        </ul>
    ]]></description>

    <change-notes><![CDATA[
        <h3>0.1.0 — Initial release</h3>
        <ul>
            <li>Interactive model-level DAG via JCEF + React Flow + dagre</li>
            <li>Column-level lineage tracing across CTEs, JOINs, UNIONs, and window functions, powered by sqlglot via a Python sidecar</li>
            <li>Reads dbt <code>manifest.json</code> + <code>catalog.json</code> (real warehouse column types when available)</li>
            <li>IDE theme integration (light / dark)</li>
            <li>Drag-to-rearrange nodes, click model card to open file, configurable hop depth (0 / 1 / 2 / 3 / 5 / 10 / ∞)</li>
            <li>Tested on DataSpell 2026.1.1; should work on any IntelliJ-based IDE on build 261+</li>
        </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow
            id="dbtree"
            anchor="right"
            factoryClass="dev.kouko.intellijdbtree.toolwindow.DbtreeToolWindowFactory"/>
        <applicationConfigurable
            parentId="tools"
            id="dev.kouko.intellij-dbtree.settings"
            displayName="dbtree"
            instance="dev.kouko.intellijdbtree.settings.DbtreeSettingsConfigurable"/>
    </extensions>

    <projectListeners>
        <listener
            class="dev.kouko.intellijdbtree.lineage.DbtFileEditorListener"
            topic="com.intellij.openapi.fileEditor.FileEditorManagerListener"/>
    </projectListeners>
</idea-plugin>
